package goyum

import (
	"testing"
)

func TestIngredients(t *testing.T) {
	/*
        appid, appkey, err := getTestingCredentials()
        if err != nil {
                t.Fatal(err)
        }

        var y *Yummly
        y, err = SetCredentials(appid, appkey)
	
	if err := y.Ingredients(); err != nil {
		t.Error(err)
	} else {
		t.Logf("yeah")
	}
	 */
}
